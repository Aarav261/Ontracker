export default function Unsubscribe() {
  // Placeholder for Phase 3 — the email-footer unsubscribe link lands here and
  // will call the backend's signed unsubscribe endpoint.
  return (
    <main className="wrap">
      <h1>Unsubscribe</h1>
      <p>Unsubscribe handling will be wired up in a later phase.</p>
    </main>
  )
}
